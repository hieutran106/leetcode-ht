from typing import List, Tuple, TypeVar
from logging import Logger

T = TypeVar('T')

from typing import NoReturn


class Solution:
    def maxIncreaseKeepingSkyline(self, grid: List[List[int]]) -> int:
        for i in range(10):
            print(i)

        result = self.function_2()
        return result


